# per-scenario wall-clock (record+render), isolated rigs, all 10 PASS
# mode: parallel — light scenarios batched at JOBS=2, then the four single-host
# ICE flows (03 06 08 09) as a SEQUENTIAL SOLO TAIL on a quiet host.
# (03/06/09 are DECOUPLE scenarios: authoritative no-recorder verify pass + a
# time-boxed best-effort cast/visual for the GIF.)
# --- batch (overlapped, ~26s wall) ---
01  3s
02  4s
04  4s
05  5s
07  6s
10  17s
# --- solo tail (sequential, the long pole) ---
03  27s
06  42s
08  7s
09  77s
TOTAL  179s
# Old fully-sequential best case was ~167s but flaky under contention. This
# layout is a reliable, deterministic 10/10. On a 4-core box JOBS auto-caps at
# nproc/2; backend_start retries on a fresh port if an eventlet boot is starved;
# every backend carries FIL_UX_RIG=1 and only our own marked procs are reaped, so
# parallel runs clean up fully between runs (no leftover-sshd starvation).
