# Filament control-plane lab. Install into the fixture venv (or any py3.10+).
python-socketio>=5.16
websocket-client>=1.9    # REQUIRED: enables the websocket transport. Without it
                         # socketio.connect(transports=["websocket"]) raises
                         # "One or more namespaces failed to connect" (the seed
                         # probe.py bug) — only polling is available otherwise.
spake2>=0.9              # SPAKE2 pairing (Ed25519), mirrors pake/src/lib.rs
