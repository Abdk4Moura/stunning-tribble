# Filament backend stack for the DigitalOcean droplet (shared box — stays small).
#   api        — Flask signaling (gunicorn+eventlet). NOT exposed to the host;
#                only reachable inside this compose network.
#   cloudflared— Cloudflare Tunnel: dials OUT to Cloudflare and routes
#                api.filament.autumated.com -> http://api:8000. Zero open ports.
#   coturn     — self-hosted STUN/TURN (host networking for the UDP relay range).
#
# Frontend is NOT here — it's static on Cloudflare Pages (see README.md).
# The image is built in CI and pushed to GHCR; the droplet only pulls it.
#
# Resource caps are env-driven (defaults below) so you can scale a single knob
# and redeploy just the api container:  API_MEM, API_CPUS, COTURN_MEM, ...
# Usage:  cp .env.example .env && edit .env && docker compose up -d

services:
  # Stateless behind Redis, so `docker compose up -d --scale api=N` (or
  # deploy/scale.sh N) spins replicas up/down in seconds. cloudflared
  # round-robins across them by the `api` service DNS name.
  api:
    image: ghcr.io/abdk4moura/filament-api:latest
    build: # only used for local builds; CI/droplet use the GHCR image
      context: ..
      dockerfile: deploy/api.Dockerfile
    restart: unless-stopped
    env_file: .env
    environment:
      FIL_ASYNC_MODE: eventlet
      # engine.io ping: the 10s/5s app default is too tight for slow/flaky
      # high-RTT clients (e.g. residential ~2.7s signaling RTT), which get
      # dropped and reconnect-storm. The liveness lease in signaling.py keeps
      # ghosts out of rosters, so a longer window is safe. Tests already use
      # 120/25 for exactly this reason.
      FIL_PING_TIMEOUT: "60"
      FIL_PING_INTERVAL: "25"
    expose:
      - "8000" # internal only — cloudflared reaches it as http://api:8000
    depends_on:
      - redis
    # OOM shield. This box runs agent workloads alongside production, and on
    # 2026-08-03 the kernel OOM killer took a systemd process and a 1.17 GB
    # rustc while production sat at oom_score ~668 with rustc at 712. A
    # forty-four point margin was the only thing that made a build die instead
    # of the signaling server, and that was luck.
    #
    # -800 puts these last in line rather than near the front. Applied here
    # rather than by hand, because a live `echo -800 > /proc/PID/oom_score_adj`
    # does NOT survive container recreation and nothing reports its absence:
    # the protection would silently lapse on the next `docker compose up -d`.
    #
    # NOTE: this covers every process in the container, including children.
    # A hand-applied shield that used `docker inspect .State.Pid` reached only
    # the gunicorn MASTER and left the single `-w 1` worker exposed, which is
    # worse than no shield: the master respawns, the container never exits,
    # `docker ps` says Up, and every live socket.io connection dies anyway.
    oom_score_adj: -800
    mem_limit: ${API_MEM:-220m}
    cpus: ${API_CPUS:-0.40}
    pids_limit: 256

  # Shared room registry + Socket.IO message queue for the api replicas.
  # Ephemeral (no persistence) — it's coordination state, not data.
  redis:
    image: redis:7-alpine
    restart: unless-stopped
    oom_score_adj: -800  # see the api service for why; -800 = last in line
    command:
      - redis-server
      - --save
      - ""
      - --appendonly
      - "no"
      - --maxmemory
      - ${REDIS_MAXMEM:-64mb}
      - --maxmemory-policy
      - allkeys-lru
    mem_limit: ${REDIS_MEM:-96m}
    cpus: ${REDIS_CPUS:-0.20}

  cloudflared:
    image: cloudflare/cloudflared:latest
    restart: unless-stopped
    oom_score_adj: -800  # see the api service for why; -800 = last in line
    command: tunnel --no-autoupdate run
    environment:
      TUNNEL_TOKEN: ${CF_TUNNEL_TOKEN}
    depends_on:
      - api
    mem_limit: ${CLOUDFLARED_MEM:-64m}
    cpus: ${CLOUDFLARED_CPUS:-0.20}

  coturn:
    image: coturn/coturn:latest
    restart: unless-stopped
    oom_score_adj: -800  # see the api service for why; -800 = last in line
    # Host networking so coturn can hand out its UDP relay port range directly.
    network_mode: host
    volumes:
      - ./coturn/turnserver.conf:/etc/coturn/turnserver.conf:ro
      # TLS cert (Let's Encrypt for turn.filament.autumated.com) for turns: on 443.
      - ./coturn/cert.pem:/etc/coturn/cert.pem:ro
      - ./coturn/pkey.pem:/etc/coturn/pkey.pem:ro
    # Secret/realm/IP come from .env so they live in exactly one place.
    # listening-ip pins coturn to the PUBLIC address only (avoids clashing with
    # tailscale and the docker bridges). 443 serves TLS-TURN (turns:, via
    # tls-listening-port in turnserver.conf) + DTLS on UDP 443 — looks like HTTPS
    # so it passes strict/throttling mobile networks that RST plain TURN.
    command:
      - -c
      - /etc/coturn/turnserver.conf
      - --static-auth-secret=${FIL_TURN_SECRET}
      - --realm=${TURN_REALM}
      - --external-ip=${DROPLET_IP}
      - --listening-ip=${DROPLET_IP}
    mem_limit: ${COTURN_MEM:-96m}
    cpus: ${COTURN_CPUS:-0.50}
